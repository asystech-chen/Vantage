# ==========================================
# Vantage Browser - Linux x64 「功能验证」快速构建配置 (DEV)
# 用途：本机日常功能 / UI / 逻辑验证，追求增量编译速度
# 基于 assets/mozconfig.new，去掉费时的 LTO / PGO
# ⚠️ 产物 ≠ 发布产物：不代表 LTO/PGO 后的性能与行为；发布构建请用 mozconfig.new
# ==========================================

ac_add_options --enable-application=browser

ac_add_options --allow-addon-sideload
ac_add_options --disable-crashreporter
ac_add_options --disable-debug
ac_add_options --disable-default-browser-agent
ac_add_options --disable-tests
ac_add_options --disable-updater
# 保留 cargo 增量编译（dev 迭代快；发布配置里是 --disable-cargo-incremental）
ac_add_options --enable-hardening
ac_add_options --enable-stl-hardening
ac_add_options --enable-optimize
ac_add_options --enable-release
ac_add_options --enable-rust-simd
ac_add_options --enable-replace-malloc
ac_add_options --enable-jemalloc

ac_add_options --with-app-name=vantage
ac_add_options --with-branding=browser/branding/vantage

ac_add_options --with-unsigned-addon-scopes=app,system

ac_add_options --with-l10n-base=$PWD/lw/l10n

ac_add_options --enable-bootstrap

export MOZ_REQUIRE_SIGNING=

mk_add_options MOZ_CRASHREPORTER=0
mk_add_options MOZ_DATA_REPORTING=0
mk_add_options MOZ_SERVICES_HEALTHREPORT=0
mk_add_options MOZ_TELEMETRY_REPORTING=0

export CFLAGS="-ftrivial-auto-var-init=zero -fwrapv -Wno-backend-plugin"
export CXXFLAGS="-ftrivial-auto-var-init=zero -fwrapv -Wno-backend-plugin"
#export LDFLAGS=

# ---- 独立 objdir：与发布构建隔离，互不干扰 ----
mk_add_options MOZ_OBJDIR=@TOPSRCDIR@/obj-x86_64-pc-linux-gnu-dev

# ---- 编译加速（无 LTO / 无 PGO） ----
mk_add_options MOZ_PARALLEL_BUILD=16
ac_add_options --enable-linker=lld
export RUSTUP_TOOLCHAIN=1.94.1

# ❌ 不启用 LTO（--enable-lto）：省去单线程、吃 20-40G 内存的全量链接
# ❌ 不启用 PGO（--enable-profile-use）：省去 profile 引导优化

if command -v ccache >/dev/null 2>&1; then
    ac_add_options --with-ccache
fi

# Add your own stuff, libraries, architecture, etc..
# ex: `cat mozconfig.orig mozconfig.my > mozconfig`
